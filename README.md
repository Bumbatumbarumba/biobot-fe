0. make sure you have node and npm installed in your system
1. cd into /biotbot-front
2. run `npm install`
3. run `npm run start` to begin the project with nx, which deploys to `http://localhost:4200/` by default
